# LowerLimbMalalignment-SGR
Segmentation-guided coordinate regression (SGR) automates landmark localization on lower-limb radiographs by combining segmentation and regression. A segmentation network predicts landmark-centered masks; these are merged with the original X-ray and processed by a regression network to estimate coordinates.
